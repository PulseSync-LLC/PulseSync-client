export interface ModInterface {
    id: number
    modVersion: string
    musicVersion: string
    checksum: string
    downloadUrl: string
    showModal: string
    createdAt: string
    changelog: string
}
