export const DESKTOP_HOST_VERSION = PULSESYNC_HOST_VERSION
export const DESKTOP_CORE_VERSION = PULSESYNC_CORE_VERSION

export interface DesktopRuntimeVersions {
    hostVersion: string
    coreVersion: string
}
